# CLAUDE.md

このリポジトリで作業するときの指示。全体方針は `~/.claude/CLAUDE.md` に従い、ここはその上書き・補足。

## このプロジェクトは何か

先行研究の紹介動画をカテゴリ別に閲覧できる**静的サイト**。
中央の球体を回してカテゴリを選び、動画を見る。学科の展示・紹介用。

## ディレクトリ

| パス | 中身 |
|---|---|
| `src/` | **本番サイト。** これ単体で完結して動く |
| `demo/` | 検討用の比較・試作ページ。本番からは独立 |
| `docs/` | ドキュメント |
| `serve.py` | 動作確認・展示用の簡易サーバー（Range 対応） |

`demo/` は `../src/` 経由で共有資産（`common.css` / `three.min.js` / `data.js` / `texture-lab.js`）を参照する。

## 絶対に守ること

### 1. ビルド工程を持ち込まない

`src/index.html` をダブルクリックするだけで動くこと。npm も bundler も入れない。
`type="module"` は `file://` で動かないので使わない（**素の `<script src>` のみ**）。

### 2. 外部への通信を増やさない

CDN 参照を足さない。Three.js は `src/vendor/three.min.js` に同梱済み（UMD 版 r149）。
Web フォントも読み込まない。テクスチャは画像ファイルではなく JS で生成する
（`file://` では画像を WebGL テクスチャにできないため）。

### 3. 重要な処理を requestAnimationFrame の完了に紐づけない

**タブが非表示だと rAF は完全に止まる。** ページ遷移・カードの表示など、
進行に関わる処理は `setTimeout` で出すこと。アニメーションは見た目だけの責務。
※ 過去に 2 回同じ不具合を作っている。

### 4. JS が毎フレーム書く CSS プロパティに transition を掛けない

`opacity` や `transform` を JS で毎フレーム更新している要素に CSS transition を足すと、
追従できずに中途半端な値で止まる。

### 5. 動画の実物を Git に入れない

GitHub は 1 ファイル 100MB が上限。`src/videos/` は `.gitignore` 済み
（`src/videos/sample/` の確認用だけ追跡）。

### 6. 本番とデモを混ぜない

`demo/` の変更が `src/` に影響しないこと。採用が決まったものだけ `src/` へ取り込む。
デモを触ったときは、`src/` のファイルが変更されていないことをコミット前に確認する。

## 低スペック PC 前提

- 静止したら描画ループを止める（`busy` が false なら rAF を再スケジュールしない）
- タブ非表示時は描画しない
- `devicePixelRatio` は 1.5 まで
- ライトオブジェクトもポストプロセスも使わない

新しく常時アニメーションを足すときは、この方針を壊していないか確認すること。

## 内容の変更

**動画は `src/videos/<カテゴリid>/` にファイルを置くだけ。** コードは触らない。
`serve.py` がフォルダを走査して `src/js/videos.js` を生成し、`data.js` がそれを読む。
ファイル名がそのままタイトルと発表者になる（`研究テーマ（山田）.mp4`）。

カテゴリの増減は **`src/js/data.js` の `categories` だけ**を編集し、
`src/videos/` に同名のフォルダを作る。
球体上の配置は個数から自動計算されるので、配置の指定項目は作らない。
詳細は `docs/data.md`。

`src/js/videos.js` は生成物。手で編集しない（serve.py に上書きされる）。

## 動作確認

### テスト（変更したら必ず実行する）

```sh
node --test test/*.test.js
```

npm は使わない。Node 標準のテストランナーだけで動く。
push（`main` / `dev`）と PR では GitHub Actions（`.github/workflows/test.yml`）が同じコマンドを回す。

- `test/data.test.js` — `src/js/data.js` の整合性と、動画一覧（`videos.js`）の中身
- `test/geometry.test.js` — 回転の計算（カテゴリが正面に来ること）
- `test/settings.test.js` — 設定画面の値の扱い
- `test/structure.test.js` — **このファイルに書いた制約の自動検査**
- `test/serve.test.js` — `serve.py` の Range と動画一覧の生成（`python3` が要る）
- `test/browser.test.js` — headless Chrome で `file://` のページを開いて動きを見る（Chrome が要る）

`python3` や Chrome が無い環境では、その検査だけ飛ばす（CI では飛ばさずに落とす）。

`structure.test.js` が落ちたときは、たいてい `file://` で開けなくなっているか、
過去に踏んだ不具合を踏み直している。メッセージに理由が書いてある。

**DOM に依存しないロジックは `src/js/geometry.js` に置く。** ここに置けばテストできる。
ブラウザでしか確かめられない動き（押したら開く、放置したら始まる）は `browser.test.js` に足す。
Puppeteer などは入れず、`test/chrome.mjs` 経由で Chrome を操作する。

### ブラウザでの確認

```sh
python3 serve.py        # /src/ が本番、/demo/ がデモ
```

`python3 -m http.server` は **Range 非対応**で動画のシークが効かないので使わない。

確認すること:
- コンソールエラーが 0 件
- 球体の回転・カテゴリのクリック・遷移
- ラベルが画面端でクランプされていないか、下部ナビと重なっていないか
- `file://`（`src/index.html` を直接開く）でも動くか

コンソールエラー・クリックと遷移・`file://` での動作は `browser.test.js` も見ているが、
見た目（ラベルのクランプや重なり）と動画の再生は人の目で確認する。

### 検証環境の制約

Claude が使う検証タブは常にバックグラウンド扱いになるため、
**rAF が止まり、`<video>` のメディア再生も始まらない**。
スクリーンショットを撮ると数フレームだけ進む。
この環境で「動画が再生されない」ことをバグと判断しないこと。
再生の確認はユーザーに依頼する。

テストが使う headless Chrome は逆に、常に表示中の扱いで rAF が動く（裏のタブにできない）。
「裏のタブで止まる」系の不具合はテストでは再現できず、`structure.test.js` の静的な検査が受け持つ。

## 動画はローカルファイルのみ

Google ドライブ埋め込みは廃止した（2026-09-10、`docs/decisions.md` 参照）。
動画は必ず `src/videos/<カテゴリid>/` に置く。
ドライブ URL・`driveId`・iframe 埋め込みは今後も持ち込まない
（別オリジンの iframe は JS から一切操作できず、自動再生・終了検知が効かないため）。

**フォルダの中身を読むのはサーバー側（`serve.py`）の仕事。**
ブラウザにディレクトリ一覧の API は無く、`file://` では `fetch` も使えない。
一覧を取るために `src/` の JS へ `fetch` を足さないこと（`structure.test.js` が禁止している）。
ファイル名を解釈するロジックは `data.js` の `parseVideoName` に置く
（Python 側に持たせるとテストできなくなるため）。

## Git

`~/.claude/CLAUDE.md` の一般方針に加えて、このリポジトリは以下に従う。

- **`main` に直接プッシュしない。** `main` はリリース用ブランチ（そのままデプロイ・配布できる状態を保つ）
- **`dev` を開発用のメインブランチとする。** 通常の作業は `dev` を起点にする
- 作業は `dev` からブランチを切り、実装後に PR を作成する。**マージ判断は利用者が行い**、
  問題なければ `dev` にマージする。`dev` → `main` への反映（リリース）も利用者の判断で行う
- **ブランチ名は `fix/作業名` の形式にする。**（例: `fix/category-filmstrip`）
- **コミットは作業単位ごとに分ける。** 1 コミットに無関係な変更を混ぜない

## 記録

設計判断と過去に踏んだ落とし穴は `docs/decisions.md` にある。
新しく判断したこと・詰まったことは、そこに追記する。
