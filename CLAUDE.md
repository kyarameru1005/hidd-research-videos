# CLAUDE.md

このリポジトリで作業するときの指示。全体方針は `~/.claude/CLAUDE.md` に従い、ここはその上書き・補足。

## このプロジェクトは何か

先行研究の紹介動画をカテゴリ別に閲覧できる**静的サイト**。
中央の球体を回してカテゴリを選び、動画を見る。学科の展示・紹介用。

## ディレクトリ

| パス | 中身 |
|---|---|
| `src/` | **本番サイト。** これ単体で完結して動く |
| `demo/` | 検討用の比較・試作ページ。本番からは独立 |
| `docs/` | ドキュメント |
| `serve.py` | 動作確認・展示用の簡易サーバー（Range 対応） |

`demo/` は `../src/` 経由で共有資産（`common.css` / `three.min.js` / `data.js` / `texture-lab.js`）を参照する。

## 絶対に守ること

### 1. ビルド工程を持ち込まない

`src/index.html` をダブルクリックするだけで動くこと。npm も bundler も入れない。
`type="module"` は `file://` で動かないので使わない（**素の `<script src>` のみ**）。

### 2. 外部への通信を増やさない

CDN 参照を足さない。Three.js は `src/vendor/three.min.js` に同梱済み（UMD 版 r149）。
Web フォントも読み込まない。テクスチャは画像ファイルではなく JS で生成する
（`file://` では画像を WebGL テクスチャにできないため）。

### 3. 重要な処理を requestAnimationFrame の完了に紐づけない

**タブが非表示だと rAF は完全に止まる。** ページ遷移・カードの表示など、
進行に関わる処理は `setTimeout` で出すこと。アニメーションは見た目だけの責務。
※ 過去に 2 回同じ不具合を作っている。

### 4. JS が毎フレーム書く CSS プロパティに transition を掛けない

`opacity` や `transform` を JS で毎フレーム更新している要素に CSS transition を足すと、
追従できずに中途半端な値で止まる。

### 5. 動画の実物を Git に入れない

GitHub は 1 ファイル 100MB が上限。`src/videos/` は `.gitignore` 済み
（`src/videos/sample/` の確認用だけ追跡）。

### 6. 本番とデモを混ぜない

`demo/` の変更が `src/` に影響しないこと。採用が決まったものだけ `src/` へ取り込む。
デモを触ったときは、`src/` のファイルが変更されていないことをコミット前に確認する。

## 低スペック PC 前提

- 静止したら描画ループを止める（`busy` が false なら rAF を再スケジュールしない）
- タブ非表示時は描画しない
- `devicePixelRatio` は 1.5 まで
- ライトオブジェクトもポストプロセスも使わない

新しく常時アニメーションを足すときは、この方針を壊していないか確認すること。

## 内容の変更

カテゴリと動画は **`src/js/data.js` の 1 ファイルだけ**を編集する。
球体上の配置は個数から自動計算されるので、配置の指定項目は作らない。
詳細は `docs/data.md`。

## 動作確認

### テスト（変更したら必ず実行する）

```sh
node --test test/*.test.js
```

npm は使わない。Node 標準のテストランナーだけで動く。

- `test/data.test.js` — `src/js/data.js` の整合性
- `test/geometry.test.js` — 回転の計算（カテゴリが正面に来ること）
- `test/structure.test.js` — **このファイルに書いた制約の自動検査**

`structure.test.js` が落ちたときは、たいてい `file://` で開けなくなっているか、
過去に踏んだ不具合を踏み直している。メッセージに理由が書いてある。

**DOM に依存しないロジックは `src/js/geometry.js` に置く。** ここに置けばテストできる。

### ブラウザでの確認

```sh
python3 serve.py        # /src/ が本番、/demo/ がデモ
```

`python3 -m http.server` は **Range 非対応**で動画のシークが効かないので使わない。

確認すること:
- コンソールエラーが 0 件
- 球体の回転・カテゴリのクリック・遷移
- ラベルが画面端でクランプされていないか、下部ナビと重なっていないか
- `file://`（`src/index.html` を直接開く）でも動くか

### 検証環境の制約

Claude が使う検証タブは常にバックグラウンド扱いになるため、
**rAF が止まり、`<video>` のメディア再生も始まらない**。
スクリーンショットを撮ると数フレームだけ進む。
この環境で「動画が再生されない」ことをバグと判断しないこと。
再生の確認はユーザーに依頼する。

## Google ドライブ埋め込みの制約

別オリジンの iframe なので JS から操作できない。
**再生位置の取得・設定、再生開始、終了検知はすべて不可能。**
自動再生が要る機能は、動画をローカルに置くか YouTube 限定公開に移す前提で設計する。

## 記録

設計判断と過去に踏んだ落とし穴は `docs/decisions.md` にある。
新しく判断したこと・詰まったことは、そこに追記する。
